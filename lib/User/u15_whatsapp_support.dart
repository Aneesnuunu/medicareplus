import 'package:flutter/material.dart';

class WhatsappSupport extends StatelessWidget {
  const WhatsappSupport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Contact  9245698745  Through the Whatsapp ",style: TextStyle(color: Colors.white),),
      ),
    );
  }
}
